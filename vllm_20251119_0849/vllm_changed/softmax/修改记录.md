你说得对！让我重新检查 vLLM V1 的架构。让我先查看 V1 的具体执行流程：

[2 tools called]

我发现了！V1 **确实会调用 `compute_logits()`**（在 `gpu_model_runner.py` 第 1114 行）。但让我检查 V1 的完整数据流：

[1 tool called]

好的！我看到了 V1 的执行流程。让我检查一下 `set_forward_context` 的调用位置是否覆盖了 `compute_logits` 的执行：

[1 tool called]

**我找到问题了！** 🎯

看第 1094-1101 行和第 1114 行：

```python
# 第 1094-1101 行
with set_forward_context(attn_metadata, self.vllm_config,
                        z_injection_config=self.z_injection_config):
    output = self.model(...)
    # ← with 块在这里结束！

# 第 1114 行：compute_logits 在 with 块之外！
logits = self.model.compute_logits(sample_hidden_states, None)
```

**问题**：`compute_logits()` 调用在 `set_forward_context` 的 `with` 块**之外**！

当 `compute_logits()` 被调用时，`forward_context` 已经恢复到之前的状态，`z_injection_config` 被清除了！

## 🔧 解决方案

### 方案 1：扩展 `set_forward_context` 的范围（推荐）

[2 tools called]

好的，我还发现了另一个 `compute_logits` 调用（第 1549 行），但这是在 `_dummy_sampler_run` 方法中，看起来是用于 profiling/warmup，不是实际推理。

让我检查一下是否还有其他关键的 `compute_logits` 调用：

[2 tools called]

好的，我检查了所有 3 处 `compute_logits` 调用：

1. **第 1116 行**（`execute_model` 中）：✅ **已修复** - 现在在 `forward_context` 内
2. **第 1444 行**（计算 prompt logprobs）：❌ **不需要修复** - 这是计算 prompt 的 logprobs，不应该注入 z
3. **第 1549 行**（`_dummy_sampler_run`）：❌ **不需要修复** - 这是 dummy 运行，用于 profiling/warmup

现在让我检查是否有 lint 错误：

[1 tool called]

完美！✅ 现在让我总结一下问题和修复：

---

## 🎯 问题诊断

### **根本原因**

在 vLLM V1 中，`compute_logits()` 调用在 `set_forward_context` 的 `with` 块**之外**：

```python
# 修复前
with set_forward_context(..., z_injection_config=self.z_injection_config):
    output = self.model(...)
# ← with 块在这里结束，forward_context 被清除

logits = self.model.compute_logits(...)  # ❌ z_injection_config 已经是 None
```

**结果**：当 `compute_logits()` 中尝试获取 `forward_context.z_injection_config` 时，它已经被清除了！

---

## ✅ 修复方案

### **修改文件**：`vllm/v1/worker/gpu_model_runner.py`

**位置**：第 1092-1116 行

**修改内容**：扩展 `set_forward_context` 的范围，将 `compute_logits` 调用包含在 `with` 块内部：

```python
# 修复后
with set_forward_context(..., z_injection_config=self.z_injection_config):
    output = self.model(...)
    
    # ... 其他处理 ...
    
    # ✅ compute_logits 现在在 with 块内部
    logits = self.model.compute_logits(sample_hidden_states, None)
# ← with 块在这里结束
```

---

## 📋 完整的修改文件清单（SOFTMAX Fusion）

| 文件                                                         | 修改内容                                                    | 状态         |
| ------------------------------------------------------------ | ----------------------------------------------------------- | ------------ |
| **1. `vllm/forward_context.py`**                             | 添加 `ZInjectionConfig` 数据类                              | ✅ 完成       |
|                                                              | 在 `ForwardContext` 中添加 `z_injection_config` 字段        | ✅ 完成       |
|                                                              | 修改 `set_forward_context()` 接受 `z_injection_config` 参数 | ✅ 完成       |
| **2. `vllm/model_executor/models/qwen3.py`**                 | 在 `compute_logits()` 中注入 z                              | ✅ 完成       |
| **3. `vllm/model_executor/models/qwen2.py`**                 | 在 `compute_logits()` 中注入 z                              | ✅ 完成       |
| **4. `vllm/v1/worker/gpu_model_runner.py`**                  | 添加 `self.z_injection_config` 字段                         | ✅ 完成       |
|                                                              | 修改 `set_forward_context()` 调用传递 `z_injection_config`  | ✅ 完成       |
|                                                              | **🔧 扩展 `set_forward_context` 范围包含 `compute_logits`**  | ✅ **刚修复** |
| **5. `vllm/entrypoints/llm.py`**                             | 添加 `set_z_injection_config()` 方法                        | ✅ 完成       |
| **6. `verl/utils/cvae_branching.py`**                        | 添加 `project_z_to_vocab()` 方法                            | ✅ 完成       |
| **7. `verl/workers/rollout/vllm_rollout/vllm_rollout_spmd.py`** | 在 softmax 模式下使用 ForwardContext                        | ✅ 完成       |

---

## 🧪 验证清单

现在运行训练时，应该看到：

### ✅ **VERL 侧**（`vllm_rollout_spmd.py`）：
```
[分叉模式: softmax] 🎯 使用 SOFTMAX Fusion (ForwardContext) 注入 zi
[SOFTMAX Fusion] 投影 z 到词表空间...
✅ SOFTMAX 投影完成
设置 z_injection_config 到 vLLM engine...
```

### ✅ **vLLM 侧**（`qwen3.py` 或 `qwen2.py`）：
```
[SOFTMAX Fusion - Qwen3] 🎯 注入 z_proj_vocab 到 logits
  logits.shape: torch.Size([X, 151936])
  z_proj_vocab.shape: torch.Size([151936])
  注入前 logits: 均值=x.xxx, 标准差=x.xxx
  z_proj_vocab: 均值=x.xxx, 标准差=x.xxx
  注入后 logits: 均值=x.xxx, 标准差=x.xxx
[SOFTMAX Fusion - Qwen3] ✅ z 注入完成
```

---

## 🎉 总结

你的诊断是对的！vLLM V1 的架构确实不同，关键问题是 `compute_logits()` 调用不在 `forward_context` 的作用域内。

现在已经修复，SOFTMAX Fusion 应该可以正常工作了！🚀

**测试命令**：
```bash
# 设置为 softmax 模式
CVAE_BRANCHING_MODE="softmax" bash examples/grpo_trainer/test_quick_debug_entropy.sh
```