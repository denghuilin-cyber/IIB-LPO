好的！我看到你已经完成了 `forward_context.py` 的修改。让我继续完成剩余的修改。

## 🚀 继续实施 PSA Fusion

[2 tools called]

现在修改 `qwen2.py`，在 Layer 循环中添加 PSA 注入：

[3 tools called]

好的！`qwen2.py` 修改完成。现在继续修改 `qwen3.py`：

[2 tools called]

现在修改 `cvae_branching.py`，添加 PSA 配置创建方法：

[2 tools called]

现在修改 `vllm_rollout_spmd.py`，将 PSA 分支改为使用 ForwardContext：

[1 tool called]

完美！所有代码修改完成。现在更新 TODO 并创建总结文档：

[1 tool called]

## ✅ PSA Fusion 实现完成！

让我创建一个完整的总结：

[1 tool called]

## 🎉 PSA Fusion (ForwardContext 版本) 实现完成！

### ✅ 修改文件清单

| 文件                                                     | 修改内容                                                     | 状态 |
| -------------------------------------------------------- | ------------------------------------------------------------ | ---- |
| `vllm/forward_context.py`                                | 扩展 `ZInjectionConfig`，添加 `z_proj_psa` 和 `psa_injection_layers` 字段 | ✅    |
| `vllm/model_executor/models/qwen2.py`                    | 在 Layer 循环中添加 PSA 注入逻辑                             | ✅    |
| `vllm/model_executor/models/qwen3.py`                    | 更新注释，说明继承自 Qwen2Model 的 PSA 支持                  | ✅    |
| `verl/utils/cvae_branching.py`                           | 添加 `set_z_injection_for_psa_fusion()` 方法                 | ✅    |
| `verl/workers/rollout/vllm_rollout/vllm_rollout_spmd.py` | 修改 PSA 分支为 ForwardContext 方案                          | ✅    |

---

### 🔒 四种模式完全互斥

```python
# mode 字段严格控制
if mode == "random":
    # 不使用 z_injection_config，完全随机生成
    new_path = self._continue_from_prefix(...)

elif mode == "input":
    # z_proj_input 有值，其他为 None
    # 在第一层 Transformer 前注入
    hidden_states += z_proj_input

elif mode == "psa":
    # z_proj_psa 和 psa_injection_layers 有值，其他为 None
    # 在指定层循环中注入（只修改最后一个 token）
    for layer_idx in layers:
        if layer_idx in psa_injection_layers:
            hidden_states[:, -1, :] += z_proj_psa

elif mode == "softmax":
    # z_proj_vocab 有值，其他为 None
    # 在 lm_head 后注入
    logits += z_proj_vocab
```

---

### 🎯 PSA Fusion 技术细节

#### 注入位置

```python
# qwen2.py 第 398-439 行
for layer_idx, layer in enumerate(self.layers[self.start_layer:self.end_layer]):
    # 正常 forward
    hidden_states, residual = layer(positions, hidden_states, residual)
    
    # PSA 注入（在指定层后）
    if z_config.mode == "psa" and actual_layer_idx in z_config.psa_injection_layers:
        hidden_states[:, -1, :] += z_proj_psa  # 只修改最后一个 token
```

#### 配置示例

```bash
# test_quick_debug_entropy.sh
ENABLE_CVAE_BRANCHING=true
CVAE_BRANCHING_MODE="psa"     # 选择 PSA 模式
CVAE_INJECTION_LAYERS=4       # 最后 4 层（或 8, "all"）
```

---

### 🧪 验证方法

运行 PSA 模式：
```bash
bash test_quick_debug_entropy.sh
# 配置：CVAE_BRANCHING_MODE="psa"
```

**预期日志**：
```
[psa] 🔒 使用 PSA Fusion (ForwardContext) 注入 zi（INPUT/SOFTMAX 已禁用）
[PSA Fusion] 🔧 准备设置 z_injection_config
  注入最后 4 层: [24, 25, 26, 27]
✅ [PSA Fusion] z_injection_config 已创建
[PSA Fusion - Qwen2Model] 🎯 在 Layer 24 注入 z_proj_psa
[PSA Fusion - Qwen2Model] ✅ Layer 24 z 注入完成
[PSA Fusion - Qwen2Model] 🎯 在 Layer 25 注入 z_proj_psa
...
✅ [psa] PSA Fusion 已清除
```

**互斥性验证**：
- ✅ 不会出现 "INPUT Fusion" 日志
- ✅ 不会出现 "SOFTMAX Fusion" 日志
- ✅ `z_proj_input` 和 `z_proj_vocab` 都为 `None`

---

### 🆚 四种模式对比总结

| 模式        | 实现机制       | 注入位置   | 注入方式                                | 跨进程支持 |
| ----------- | -------------- | ---------- | --------------------------------------- | ---------- |
| **random**  | 无注入         | -          | 完全随机                                | ✅          |
| **input**   | ForwardContext | 第一层前   | `hidden_states += z_proj_input`         | ✅          |
| **psa**     | ForwardContext | 指定层后   | `hidden_states[:, -1, :] += z_proj_psa` | ✅          |
| **softmax** | ForwardContext | lm_head 后 | `logits += z_proj_vocab`                | ✅          |

---

### 🎊 总结

**PSA Fusion 已完全实现并与其他模式完全互斥！**

- ✅ 使用 ForwardContext 机制（与 INPUT/SOFTMAX 一致）
- ✅ 支持多进程环境（Ray Worker）
- ✅ 严格的互斥性保证（通过 `mode` 字段）
- ✅ 可配置注入层数（4, 8, "all"）
- ✅ 完整的日志输出

**现在可以测试 PSA 模式了！** 🚀